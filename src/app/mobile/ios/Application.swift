import SwiftUI

struct Application: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct Application_Previews: PreviewProvider {
    static var previews: some View {
        Application()
    }
}